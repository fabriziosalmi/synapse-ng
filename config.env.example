# Esempio di configurazione per un nodo Synapse-NG

# La porta interna su cui il server FastAPI ascolterà.
NODE_PORT=8000

# L'URL pubblico completo di questo nodo, usato per registrarsi con il Rendezvous server.
# In un contesto Docker, questo dovrebbe usare il nome del servizio.
OWN_URL=http://node-1:8000

# L'URL del Rendezvous server per il discovery dei peer.
RENDEZVOUS_URL=http://rendezvous:8080

# Lista di canali tematici a cui iscriversi all'avvio, separati da virgola.
# Il canale 'global' è sempre sottoscritto di default e non è necessario specificarlo.
SUBSCRIBED_CHANNELS=sviluppo_ui,marketing
